import React from 'react';

const CartWidget = () => {
    return (
        <div>
            <img className="cartLogo" src="/img/Cart.svg" alt="" srcset="" />
        </div>
    );
}

export default CartWidget;
